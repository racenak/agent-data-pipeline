deployments:
  - name: hello-deploy
    entrypoint: flow/hello-world.py:hello
    work_pool:
      name: local-pool

  - name: etl
    entrypoint: flow/etl.py:main_etl_flow
    work_pool:
      name: local-pool
      job_variables:
              env:
                EXTRA_PIP_PACKAGES: "clickhouse-connect"
