apiVersion: 1

datasources:
  - name: DS_PROMETHEUS
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: DS_LOKI
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
